# Node
node_modules/

# Dane generowane z markdownu (npm run build:materials)
src/data/generated/

# Zrzuty referencyjne / lokalne APK
baseline-shots/

# Android build outputs & machine-specific config
android/.gradle/
android/build/
android/app/build/
android/local.properties
*.apk

# Generated verification artifacts
verify/
android_screenshot*.png
planmode_current_screen.png
qr_*.png

# Local tooling / cache
.mimosa/
.zcode/
wakelaptop

# Reference photos (not needed to build or run)
IMG_*.jpg

# Build Vite trafia bezposrednio do assets WebView (budowany w CI, nie w git)
android/app/src/main/assets/www/
